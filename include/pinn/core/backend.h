/*
 * core/backend.h
 *
 * Owns backend/device declarations. This will eventually expose CPU/CUDA
 * dispatch helpers, tensor_to_cpu, tensor_to_cuda, device synchronization, and
 * backend capability checks.
 */
