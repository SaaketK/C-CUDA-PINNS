/*
 * pinn/trainer.h
 *
 * Owns PINNProblem and training configuration declarations. The trainer will
 * sample points, evaluate model derivatives, compute residual/constraint losses,
 * run backward, step the optimizer, and log training progress.
 */
